<Anuncio1Window>:
    name: "anuncio1"
    tiendas_layout:tiendas_layout

    FloatLayout:
        canvas.before:
            Color:
                rgba:(241/255,228/255,232/255,1)
            Rectangle:
                pos: self.pos
                size: self.size


        BoxLayout:
            pos_hint: {"top":0.9}
            size_hint: 1, 0.75
            orientation: 'vertical'
            padding: 10, 10
            row_default_height: '400dp'
            row_force_default: True
            spacing: 10, 100

            GoBackBut:
                size_hint: 0.1, 0.1
                on_release:
                    app.root.current = "menucompras"
                    root.manager.transition.direction = "right"


            Label:
                text:"[b]Tiendas[/b]"
                markup: True
                halign: 'center'
                valign: 'center'
                font_size: (root.width**2 + root.height**2) / 13**4
                pos_hint: {"x":0, "top":1}
                size_hint: 1, 0.1
                color: 37/255,65/255,178/255,1
                text_size: self.size
            Label:
                text:"[font=Arial]Seleccione las tiendas que visitará[/font]."
                markup: True
                halign: 'center'
                valign: 'center'
                font_size: (root.width**2 + root.height**2) / 15**4
                pos_hint: {"x":0, "top":0.94}
                size_hint: 1, 0.1
                color: 0,0,0,1
                text_size: self.size
            ScrollView:
                pos_hint: {"y":0.7}
                GridLayout:
                    row_default_height: '60dp'

                    id:tiendas_layout
                    cols:1
                    size_hint_y: None
                    height:self.minimum_height
                    spacing: 0, 5
                    padding: 16, 10

        GridLayout:
            cols:1
            pos_hint: {"top":0.15}
            size_hint: 1, 0.15
            AnchorLayout:
                Button:
                    size_hint: 0.4, 0.3
                    text: "[font=Arial]Siguiente[/font]"
                    markup: True
                    font_size: (root.width**2 + root.height**2) / 15**4
                    background_color: 81/255,255/255,85/255,1
                    on_release:
                        root.manager.transition.direction = "left"
                        root.gather_go()
